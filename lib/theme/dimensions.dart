import 'package:flutter/material.dart';

class AppDimensions {
  static const double paddingTop = 80;
  static const double paddingHorizontal = 32;
  static const double paddingBottom = 40;

  static const double radius = 16;
  static const double buttonHeight = 48;
  static const double buttonRadius = 16;

  static const EdgeInsets top = EdgeInsets.only(top: paddingTop);
  static const EdgeInsets horizontal = EdgeInsets.symmetric(horizontal: paddingHorizontal);
  static const EdgeInsets bottom = EdgeInsets.only(bottom: paddingBottom);
}
